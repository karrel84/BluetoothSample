<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="@dimen/byte_box_layout_size"
        android:orientation="vertical">

        <TextView
            android:id="@+id/number"
            style="@style/baseText.byteBox"
            android:layout_width="@dimen/byte_box_size"
            android:layout_height="@dimen/byte_box_size"
            tools:text="0" />

        <EditText
            android:id="@+id/value"
            style="@style/baseText.byteContent"
            android:layout_width="@dimen/byte_box_size"
            android:layout_height="@dimen/byte_box_size"
            android:maxLength="2"
            android:maxLines="1"
            android:singleLine="true"
            tools:text="AC" />
    </LinearLayout>
</layout>